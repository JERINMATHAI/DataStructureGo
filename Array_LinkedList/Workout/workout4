package main

import "fmt"

func fact(num int) int {
	if num < 1 {
		return 1
	}
	return num * fact(num-1)
}
func main() {
	var num int
	fmt.Println("Enter the number")
	fmt.Scan(&num)
	factorial := fact(num)
	fmt.Println(factorial)

}
